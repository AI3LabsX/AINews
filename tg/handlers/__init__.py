from tg.handlers.handlers import (
    add_handler,
    delete_handler,
    button_handler
)


HANDLERS: tuple = (  # the order of the elements is important
    add_handler,
    delete_handler,
    button_handler
)
